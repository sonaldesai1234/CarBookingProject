version: '2'
services:
    mycipl-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb mycipl-mongodb-config
    mycipl-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    mycipl-mongodb-config:
        image: mongo:4.0.2
        container_name: mycipl-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
